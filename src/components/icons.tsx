import {ChevronLeft, Command, Loader2, LucideIcon,} from "lucide-react"

export type Icon = LucideIcon

export const Icons = {
  logo: Command,
  create: Command,
  spinner: Loader2,
  chevronLeft: ChevronLeft,
}
